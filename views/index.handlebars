<!-- search bar -->
<div class="container search-bar">
  <div class="search-bar-outer">
    <div class="search-bar-inner">
      <form action="/search">
        <h4 class="mb-2">尋找餐廳：</h4>
        <div class="input-group">
          <input type="text" name="keyword" class="form-control" placeholder="輸入餐廳、分類" value="{{keyword}}"
            aria-label="Restaurant Name..." value="" aria-describedby="search-button">
          <div class="input-group-append">
            <button class="btn btn-major" type="submit" id="search-button"><i class="fas fa-search"></i></button>
          </div>
        </div>
      </form>
    </div>
  </div>
</div>
<!-- restaurant list -->
<div class="container mt-5">
  <div class="row">
    <div class="col-md-10 col-12 mx-auto">
      <div class="row">
        {{#each restaurants}}
        <div class="col-sm-4">
          <a href="/restaurant/{{this._id}}" class=" text-secondary">
            <div class="card mb-3">
              <img class="card-img-top" src="{{this.image}}" alt="{{this.name}}">
              <div class="card-body p-3">
                <h6 class="card-title mb-1">{{this.name}}</h6>

                <div class="restaurant-category mb-1">
                  <i class="icon fas fa-utensils"></i> {{this.category}}
                </div>
                <div class="badge rounded-pill bg-danger font-weight-normal col-3">
                  {{this.rating}}
                  <i class="fas fa-star fa-xs"></i>
                </div>
                <div class="d-flex justify-content-end">
                  <div class="me-1"><a href="/restaurant/new"><i class="fas fa-plus"></i></a></div>
                  <div class="me-1"><a href="/restaurant/edit"><i class="fas fa-edit"></i></a></div>
                  <div class="me-1"><a href="/restaurant/delete"><i class="fas fa-trash-alt"></i></a></div>
                </div>
              </div>
            </div>
          </a>
        </div>
        {{/each}}
      </div>
    </div>
  </div>
</div>